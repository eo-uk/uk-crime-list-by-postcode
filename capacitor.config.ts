import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.co.ukcrimelookup.app',
  appName: 'uk-crime-lookup',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
